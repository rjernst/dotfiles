#!/usr/bin/env zsh

for i in {7..18}; do
  JDK_HOME=/usr/lib/jvm/java-${i}-jdk
  if [ -d $JDK_HOME ]; then
    export JAVA${i}_HOME=$JDK_HOME
  else
    JDK_HOME=/usr/lib/jvm/java-${i}-openjdk
    if [ -d $JDK_HOME ]; then
      export JAVA${i}_HOME=$JDK_HOME
    fi
  fi
done
