/**
 * Provider registration stub — full implementation in Step 3.
 */
import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

export function registerClaudeSdkProvider(_pi: ExtensionAPI): void {
	// Implementation added in Step 3
}
