* add readme
* redo license
* redo prompt
    - need time
    - other git status
    - structure
    - status code
    - special prompt char
* add macos bootstrap
    - brewfile
    - extract checkout code
    - extract ssh keygen
* keybase setup (after package install)
    - gpg key
    - git commit signing
* config aliases
* tmux tab names
* tmux window name
* terminal window name
