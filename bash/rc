
[[ -r ~/.bash_rc.local ]] && source ~/.bash_rc.local

[[ -r ~/.bash/env.local ]] && source ~/.bash/env.local
source ~/.bash/env
[[ -r ~/.bash/env.override ]] && source ~/.bash/env.override

[[ -r ~/.bash/aliases.local ]] && source ~/.bash/aliases.local
source ~/.bash/aliases
[[ -r ~/.bash/aliases.override ]] && source ~/.bash/aliases.override

source ~/.bash/git_completion
source ~/.bash/iterm2_integration

set -o noclobber       # can't > to existing files
set -o ignoreeof       # ignore ^D to exit shell
set -o notify          # report change in jobs' status
set -o vi              # vi commands

# ^p check for partial match in history
bind -m vi-insert "\C-p":dynamic-complete-history
# ^n cycle through the list of partial matches
bind -m vi-insert "\C-n":menu-complete

shopt -s cdspell
shopt -s checkhash
shopt -s checkwinsize
shopt -s histappend
shopt -s no_empty_cmd_completion

PS1="\[\033]0;${TITLE} \a\]${PROMPT}" 

# use xrs (resize) to reset your window size after
# you have resized an xterm (is this needed?)
alias xrs='set -o noglob; eval `resize`; set +o noglob'

[[ -r ~/.bash/rc.override ]] && source ~/.bash/rc.override

# vi: set tabstop=4 shiftwidth=4 textwidth=79 filetype=sh expandtab:
