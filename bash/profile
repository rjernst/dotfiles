
[[ -r ~/.bashrc.local ]] && source ~/.bashrc.local
source ~/.bashrc
[[ -r ~/.bashrc.override ]] && source ~/.bashrc.override
source ~/.bash/iterm2_integration

if [[ "$AUTO_TMUX" -eq "1" && -z "$TMUX" ]]; then
  tmux has-session 2> /dev/null
  if [ "$?" -eq 1 ]
  then
    echo "Starting tmux session"
    tmux -CC
  fi
  tmux -CC attach
fi

